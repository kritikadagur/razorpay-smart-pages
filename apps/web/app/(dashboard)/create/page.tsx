import { WizardShell } from "@/components/wizard/WizardShell";

export const metadata = {
  title: "Create Smart Page — Razorpay",
  description: "Build a conversion-optimized payment page in under 2 minutes",
};

export default function CreatePage() {
  return <WizardShell />;
}
